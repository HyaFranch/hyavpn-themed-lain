# Usado apenas para desenvolvimento e para o build automático (PyInstaller/CI).
# O usuário final NÃO precisa instalar nada disso — ele só baixa o installer.
pywebview>=4.4.0
Pillow>=10.0.0
psutil>=5.9.0
pygame>=2.5.0
pyinstaller>=6.0.0

# pywebview no Windows usa WebView2 (Chromium) por padrão.
# Para desenvolvimento, também instale o runtime WebView2 (já vem no Windows 11;
# no Windows 10 pode ser necessário: https://developer.microsoft.com/webview2)
